
## PATH ##

export PATH=$PATH:~/bin:.

# Include gem directory in path and setup RVM.
export PATH=`gem env gemdir`:$PATH
[[ -s $HOME/.rvm/scripts/rvm ]] && source $HOME/.rvm/scripts/rvm

# Include Chromium depot_tools
# export PATH=/usr/local/lib/depot_tools:$PATH

# MySQL Path
# export PATH=/usr/local/mysql/bin:$PATH

# Use this as an indication that can be picked up from the gem file to load the ruby debug gem.
# export LOAD_RDBUG19_GEM=true
